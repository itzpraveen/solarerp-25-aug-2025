### Summary

- [ ] What change does this PR introduce? (1–2 lines)

### Preview

- [ ] Vercel Preview URL: <!-- paste preview URL here or say “auto” if the workflow posts it -->

### Checks

- [ ] `npm test` passes locally
- [ ] `npm run build` passes locally (or `vercel build`)
- [ ] No console errors in Preview for the changed pages
- [ ] If DB changes: added SQL under `drizzle/` and noted in “Migrations”
- [ ] If env changes: updated `.env.example` and noted in “Env”

### Migrations (if any)

<!-- List SQL files added/changed under `drizzle/` and special rollout notes -->

### Env (if any)

<!-- List new/changed env vars and which scopes (Preview/Prod) need them -->

